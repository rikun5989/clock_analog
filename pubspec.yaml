name: clock_analog
description: Use clock_analog widgets to pick time of day (without dialog box) from main screen.

version: 0.0.3
homepage: https://github.com/rikun5989/clock_analog

environment:
  sdk: ">=2.17.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
